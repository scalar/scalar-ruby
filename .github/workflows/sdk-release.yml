name: Ruby SDK Release

on:
  # Manual re-publish only: the automated publish for each release runs as the `publish` job
  # inside release-please.yml, in the same run that cuts the tag (dispatching a workflow here
  # instead would resolve it on the repository's default branch only, which breaks releasing
  # from any other release line). Dispatch this workflow at an existing release tag to re-run
  # a failed or skipped publish. A dispatched run is top-level, so OIDC trusted-publisher
  # claims name this file; register it as an additional trusted publisher only when manual
  # re-publishes are used. Never add a workflow_call trigger here — registries reject
  # publishes from called (reusable) workflows.
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      VERSIONING_POLICY: manual
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: ruby/setup-ruby@9eb537ca036ebaed86729dcb9309076e4c5c3b74 # v1.314.0
        with:
          ruby-version: "3.2"
          bundler-cache: false
      - run: ruby -e 'Dir["lib/**/*.rb"].each { |file| system("ruby", "-c", file) || exit(1) }'
      - run: gem build *.gemspec
      # Trusted publishing: exchanges the job's OIDC id-token for a short-lived rubygems.org API
      # key and exports it as GEM_HOST_API_KEY for the push below, so no long-lived secret is
      # stored. Requires a trusted publisher registered for this gem on rubygems.org naming this
      # repository and this workflow file (a pending publisher covers a gem's first release).
      - name: Configure RubyGems credentials
        uses: rubygems/configure-rubygems-credentials@dc5a8d8553e6ee01fc26761a49e99e733d17954a # v2.1.0
      - name: Publish to RubyGems
        run: |
          gem build *.gemspec --output scalar-ruby.gem
          GEM_VERSION="$(ruby -e 'puts Gem::Specification.load(Dir.glob("*.gemspec").first).version')"
          if curl -sf -A "scalar-sdk-release" "https://rubygems.org/api/v1/versions/scalar-ruby.json" | grep -q "\"number\":\"${GEM_VERSION}\""; then
            echo "scalar-ruby ${GEM_VERSION} already on RubyGems, skipping"
          else
            gem push scalar-ruby.gem
          fi
